# Development/QA tooling (runtime deps come from the distro; see
# tools/install-deps.sh). Floors, not pins — Dependabot proposes updates
# monthly; CI catches breakage.
ruff>=0.16.0
mypy>=2.3.0
import-linter>=2.14
pytest>=9.1.1
pytest-asyncio>=1.4.0
# NOTE: PyGObject-stubs deliberately absent — it drags in a from-source
# PyGObject build (C toolchain) in clean containers; mypy handles gi.* via
# ignore_missing_imports instead (pyproject.toml).
